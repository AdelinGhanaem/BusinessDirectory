package com.businessdirecotory.server.authorization;

import org.junit.Test;

/**
 * @author Adelin Ghanayem <adelin.ghanaem@clouway.com>
 */
public class SecurityTokenBuilderImplTest {


  private IdGeneratorImpl builder = new IdGeneratorImpl();

  @Test
  public void returnsSecurityToken() {
    String username = "username";
//    Token token = builder.generateId();
//    assertThat(token, is(notNullValue()));
//    assertThat(token.getUser(), is(username));
//    assertThat(token.getExpireDate(), is(notNullValue()));
//    assertThat(token.getUserId(), is(notNullValue()));
  }

  @Test
  public void test() {

  }
}
